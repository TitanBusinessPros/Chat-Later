{
  "versionCode": 18,
  "versionName": "1.17",
  "apkUrl": "https://github.com/TitanBusinessPros/Chat-Later/releases/latest/download/Chat-Later.apk",
  "notes": "Fixed a second Google Sign-In failure path (the Firebase signInWithCredential step, separate from the account-picker step fixed in v1.16) that could still show nothing on a blank error message. Added a catch-all so any unexpected error in Google Sign-In always shows on screen with its exception type and message, instead of ever failing silently."
}
